# Second Ansible task for DevOps school
### Example of loops and Ansible Vault usage
Command to run this playbook:
```
ansible-playbook playbook.yml --vault-password-file password_for_ansible_vault.txt
```
